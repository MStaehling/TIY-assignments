{
  "name": "ViewModeSwitch",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "browser-sync start --server --files='js/**/*.js,test/spec/**/*.js' --startPath test/index.html",
    "start": "browser-sync start --server --files='js/**/*.js, css/*.css, index.html'"
  },
  "repository": {
    "type": "git",
    "url": "\"test\": \"browser-sync start --server --files='js/**/*.js,test/spec/**/*.js' --startPath test/index.html\","
  },
  "author": "",
  "license": "CC0"
}
